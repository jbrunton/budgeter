<%= form_with(model: statement, local: true) do |form| %>
  <% if statement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(statement.errors.count, "error") %> prohibited this statement from being saved:</h2>

      <ul>
      <% statement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filename %>
    <%= form.text_field :filename, id: :statement_filename %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :statement_content %>
  </div>

  <div class="field">
    <%= form.label :first_date %>
    <%= form.date_select :first_date, id: :statement_first_date %>
  </div>

  <div class="field">
    <%= form.label :last_date %>
    <%= form.date_select :last_date, id: :statement_last_date %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :statement_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
